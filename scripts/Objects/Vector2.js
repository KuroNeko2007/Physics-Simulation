class Vector2 {
    constructor(x, y) {
        this.x = x;
        this.y = y;
    }
    
    /**
     * @param {number} a
     * @param {number} b
     * @returns {Vector2}
     */
     /**
      * @param {Vector2} a
      * @returns {Vector2}
      */
    add(a, b) {
    	
    }
}